[phases.setup]
nixPkgs = ["nodejs_22", "npm"]

[phases.install]
cmds = ["cd backend && npm ci --production || cd backend && npm install --production"]
dependsOn = ["setup"]

[start]
cmd = "cd backend && npm start"
